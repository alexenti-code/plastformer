# PlastFormer Constitutional Audit — v0.5 / E1 v1.1 / PMI executor v0.6.0

Date: 2026-09-05. Auditor role: constitutional (owner: Alex).
Normative base: `/Users/alex/matryoshka/GLOSSARY.md` axioms 1–9; `plastformer/docs/ADR-001-plastformer-transition.md` (binding); theory interpretation "layers are speeds", "temperature-class dials only".
Artifacts audited (full read, no edits): `plastformer/preprint.md` (v0.5), `plastformer/experiments/e1-protocol.md` (v1.1), `matryoshka-mmi/mmi_mcp.py` (v0.6.0) + `matryoshka-mmi/SPEC.md`.
Severity scale (per task): VIOLATION = external decider of meaning / rule / trigger / ranker present in a main path; AMBER = borderline, needs explicit justification or relabeling; OK = pass (only strongest 3 noted).
Quotes are exact (1–3 lines). No flattery.

## Verdicts per artifact

### A. preprint.md v0.5 — CONDITIONAL PASS (3 VIOLATION, 6 AMBER)
Architecture text is honest about limits (tamper-evident not rewrite-proof, training shapes acts, dormancy limits). But it authorizes two external mechanisms in main-path language: auto-write unconscious register and outside-owned reminder timing/delivery. Deterministic fact extraction is a fourth external semantic rule. Ablation hygiene is good (RAG ranker, wall-clock decay, unconscious-surrogate named as external controls). Fix by narrowing three sentences and relocating auto-write to ablation.

### B. experiments/e1-protocol.md v1.1 — FAIL ON ONE POINT, otherwise STRONG (1 VIOLATION, 4 AMBER)
v1.1 fixes most v1.0 problems explicitly (relevance ranker removed, surrogate off, timestamps on Arm B, act log, honest labeling). One main-path violation remains: automatic loudest-N injection before every user turn without model request. Tick definition is internally contradictory (per-exchange vs per-act). Provenance/friction tables are fixed pre-run policy but need non-veto framing plus a no-feedback rule for the judge. Fix by making injection model-gated or explicitly justified as metered physics with opt-out.

### C. stand code (mmi_mcp.py) + SPEC.md v0.6.0 — CONDITIONAL PASS (0 VIOLATION, 5 AMBER)
Code contains zero embeddings, zero relevance ranking, zero content-based deletion, zero auto-write to PHI. This is the cleanest artifact. Issues are defaults and side channels, not rankers: default layer assignment, WRITE-only injection filter, update-notice injection into tool results, unimplemented hash-chain/friction/provenance claims from the papers, and `last`-vs-archive visibility decided outside the model. Fix with small code/text changes listed below.

Counts: VIOLATION 4 / AMBER 12 / OK 3.

## Findings table

| ID | Sev | Artifact + section | Exact quote | Axiom + why | Fix (concrete) |
|----|-----|--------------------|-------------|-------------|----------------|
| F1 | VIOLATION | e1-protocol §5 Reads, Physics channel | "before each user turn, the stand injects the N records with the largest current weight as a `<<PMI>>` block (N = `MMI_INJECT_TOP`, ≤ 2k tokens). Selection by amplitude only. No embeddings, no keyword match, no query-dependence, no search by content. This is a content-blind injection, not retrieval." | Ax.2 (model is engine of own memory; each act is conscious act) + Ax.9 (model owns ticks, decides what to spend them on). Selection is content-blind, but the TRIGGER (WHEN, every turn, unrequested) is decided by the stand/harness. Same paragraph asserts the opposite: "The model decides when to read and what to ask for." Both cannot hold. Content-blindness does not cure an external trigger. | Pick one. Preferred: make injection model-gated. Reword to: "The stand never injects. On each `matryoshka_read` the executor appends a `<<PMI>>` block with the N loudest traces by weight; the model decides whether to call `read` and with what N-cap. The harness MUST NOT call `read` or paste `<<PMI>>` before a user turn unless the model asked." Alternative if auto-reminder is wanted: keep auto-injection ONLY as a registered ablation `loudest-N-auto on`, add justification (metered physics, fixed budget, model can ignore/close), add opt-out act (`ignore-pmi`), log every injection in act log, and exclude from PR1–PR4 main claim. Also fix code/harness gap: current `mmi_mcp.py` only attaches `_pmi` to `read` results; "before each user turn" must be harness behavior — forbid it there. |
| F2 | VIOLATION | preprint §1 syntax/semantics split | "storage, delivery, timing, logging, pricing and resource limits are *syntactic* functions that may live outside; importance, contradiction, connection, recall, and reconciliation of clocks are *semantic* acts that must live inside" | Ax.9 + Ax.2. Listing "timing" and "delivery" as outside-legitimate authorizes F1: who decides WHEN a reminder is delivered is a semantic decision about recall (explicitly listed as inside in the same sentence). The split contradicts itself and the axioms. | Narrow the outside list. Reword to: "storage, logging, metered transport, pricing and resource caps are syntactic and may live outside; timing and delivery of reminders (when/what surfaces), importance, contradiction, connection, recall, and reconciliation are semantic and must be the model's acts. The environment may attach metered loudest-N context ONLY in response to the model's own `read`." Then F1 becomes an instance of this rule. |
| F3 | VIOLATION | preprint §3.3 Unconscious register | "Every experience that passes the window leaves a low-amplitude trace in fast components, gated by a surprise signal in the spirit of Titans [1]." + "surprise is the *core's* prediction error — a semantic signal from inside the model — so the unconscious register is physics in its *writing*, but its gate is semantic." | Ax.2 (every act of remembering is a conscious act of the model). "Every experience leaves a trace" is an auto-write rule: no model decision per trace. The escape clause ("gate is the core's own error") does not hold on the symbolic stand — E1 itself admits: "The core's own prediction error is not accessible on this stand." On the stand the gate can only be an external classifier (the rejected surrogate). A parametric-future gate does not license a symbolic-present auto-write. | Relocate to ablation/future. Reword §3.3 to: "Unconscious auto-write is NOT part of the claimed architecture on the symbolic stand. Main path: conscious acts only. A core-gated unconscious register is a parametric-future variant, admissible only when the gate is provably the core's own prediction error and never an external score; on the symbolic stand any embedding-distance gate is the `unconscious-surrogate` ablation." Keep ADR-001 §4.3 / E1 "off in main run" as the binding reading. |
| F4 | VIOLATION | preprint §3.7 Provenance, hard-fact path | "Hard facts (dates, sums, identifiers) enter verbatim through deterministic extraction, never through paraphrase." | Ax.1 (only K does interpretation) + Ax.2. Deciding "this span is a hard fact → verbatim, else paraphrase-eligible" is a semantic classification of content. "Deterministic" does not make it non-semantic; a regex for dates/sums/ids still decides meaning from outside. Combined with "The trust class of a source sets $a_i(0)$." + "The environment weighs the source; it does not interpret the content." the environment both classifies content and sets amplitude. | Remove auto-extraction from the main path. Reword to: "No extraction component writes to Φ. The model issues `name` with verbatim content when it judges fidelity required; provenance records source class as declared by the model subject to deployer policy caps. Any deterministic pre-extractor, if used, is an experimental harness ablation (`verbatim-extractor on`), its outputs are proposals the model must confirm by act, and it never sets amplitude." Add the missing implementation note: current executor does not implement $a_0$ weighting (see F8) — do not claim it as built. |
| F5 | AMBER | preprint §3.4 Read path + E1 §5 Act channel | "Retrieval injects decaying traces before attention." + "(the read itself is the model's act; see E1 in §7)" | Ax.2/Ax.9. Same sentence claims injection (passive model) and act (active model). In parametric form "injects before attention" can be legitimate trained-interface physics; in symbolic form it reads as F1 auto-reminder. | Disambiguate per substrate. Reword to: "Parametric: traces enter pre-attention as vectors through the trained interface (physics). Symbolic stand: nothing enters except in response to the model's `read`; the `<<PMI>>` block is a `read` response field, never a pre-turn push." Cross-ref the F1 fix. |
| F6 | AMBER | preprint §3.5 Tick vs E1 §4–5 Tick vs code | preprint: "One tick = one inference step (one generation batch)." / E1 §5: "One executed memory act (WRITE/REPEAT/CONNECT/RECONCILE) = +1 tick." / E1 §4: "In E1 one user message is one exchange; the stand's lived-tick counter advances once per executed memory act (§5, Tick), so it tracks exchanges monotonically." / E1 Changes: "Δn in lived ticks (1 tick = 1 exchange, counted by the stand)" | Ax.9 + P1 honesty. Three definitions (inference step / storing act / exchange) are used interchangeably. Code implements per-storing-act (`_auto_tick` on write/repeat/connect/reconcile; READ/STATUS never advance). "Tracks exchanges monotonically" is false when exchanges without acts do not advance (E1 §5 admits this two sentences later). P1 (age from amplitude, interval length from tick count) cannot be tested if Δn unit is unstable. | Freeze one definition and propagate. Recommended (matches code): "1 tick = 1 executed storing act, counted by the stand; exchanges/probes without storing acts advance nothing." Fix E1 §4 to: "message_no orders the ledger; Δn uses the stand counter only; the two are not interchangeable." Fix E1 Changes line to per-act wording. Fix preprint §3.5 with a stand-mapping note: "Abstract tick = one inference step; symbolic-stand counter = one storing act (a sparse sampling of abstract ticks); E3 must state which counter is used." Add a run-manifest check: report ticks-per-100-exchanges distribution. |
| F7 | AMBER | code `act_write` / `act_connect` defaults + `_weight` fallback | "def act_write(content, layer=\"episode\", valid_time=None, source=\"dialogue\") -> dict:" / "def act_connect(refs, summary, layer=\"episode\") -> dict:" | Ax.6 (layers are speeds; further division is the model's decision). When the model omits `layer`, the executor assigns `episode`. A default is an external temporal-scale decision. Same for `tau_ticks().get(rec.get("layer", "episode"), tau_ticks()["episode"])` and `TAU.get(rec.get("layer", "episode"), TAU["episode"])` fallbacks. Low risk (model can override), but it is content-adjacent governance by omission. | Require explicitness. Code: default `layer=None`; if None, return an error asking the model to choose (`error: layer required: beat|episode|day|project|life`), or record `layer: "unspecified"` with neutral τ (e.g. episode τ but labeled as harness default, excluded from layer-based claims). At minimum change tool descriptions to "layer is required; there is no architectural default; `episode` shown is a client convenience." SPEC §4 example `"layer": "episode"` must be labeled example-only. |
| F8 | AMBER | E1 §5 Provenance table + preprint §3.7 weighting; code gap | "Initial amplitude by source class: user statements a_0 = 1.0; model's own summaries (`connect`) 0.6; retrieved external docs 0.4. Class assignment is deployer policy and is fixed before the run." + preprint: "The trust class of a source sets $a_i(0)$." | Ax.2. Externally set $a_0$ is external distribution of weight. Fixed-before-run policy + class-based multiplier is closer to a temperature dial than a per-trace judge, but the protocol never states who labels each record's class at runtime (model self-declares? executor infers from tool/args? harness overrides?). Code gap: `_weight` computes `(1 + repeats) * exp(-dt/tau)` with NO $a_0$ factor — provenance is stored (`source` string) but ignored. Paper claims built physics the stand does not implement. | Two-part fix. (a) Constitutional framing: "Class table is a pre-registered deployer cap, fixed before the run; per-record class is asserted by the model in the `name` call; the executor never reclassifies content; weighting is `min(model_asserted, cap)` or model-asserted only. Any harness override is forbidden in the main run." (b) Implementation honesty: either implement $a_0$ in `mmi_mcp.py` + SPEC before claiming it, or downgrade text to "E1 main run uses uniform $a_0=1$ (provenance recorded, not weighted); class weighting is a `provenance-weighting on` ablation." Add ablation `with/without provenance weighting` (already in preprint §7 list) to the E1 ablation table explicitly. |
| F9 | AMBER | E1 §5 Friction; SPEC friction "shown, not enforced" | "Write cost c(component) fixed by the environment; `repeat` and `connect` into slow components cost more (enforced as a token/compute budget per act). The model decides whether to pay; it never sets the price." | Ax.2 (external veto risk). A budget "enforced" by the stand is an external refusal to execute the model's act. As pure physics (finite tokens/compute) it is legitimate; as a per-act semantic toll it needs a veto-free formulation. Code gap: no enforcement exists (`matryoshka_status` only reports the friction model; `call_tool` never rejects). | Clarify enforcement as physics, implement or drop the claim. Reword to: "Friction is a pre-registered metered cost (extra tokens/compute charged per slow-component write), never a refusal: the executor always executes a well-formed act and meters it; over-budget runs are reported, not blocked. Any refusal mode is a separate `friction-veto` ablation." Either implement metering in the executor manifest log or change "enforced" to "metered/reported" until implemented. |
| F10 | AMBER | preprint §7 E5 oracle + §3.3 training boundary | "Precision/recall of `name`/`repeat`/`connect` against an oracle, with the caveat that the oracle defines the training target and therefore bounds, rather than measures, governance." + "what the model learns to repeat is shaped by whatever the training signal rewarded." | Ax.1/Ax.2. Training TO the oracle makes the oracle the external shaper of what gets repeated. The caveat is honest but does not contain the risk: a future reader can still cite E5 as governance evidence, and nothing forbids importing the oracle (or ledger) into the run loop as a rewarder/filter. | Add a non-feedback rule and a held-out test. Insert in §7 E5: "The oracle/ledger never enters Φ, never triggers `repeat`, never filters acts during a run; it scores post-hoc only. Governance is assessed on held-out biographies where no oracle exists (E1–E3 on novel corpora), not on oracle overlap. Training on oracle-shaped rewards is capacity scaffolding; any claim that E5 measures self-governance is explicitly disclaimed." Mirror one sentence in E1 §7–8: "Judge/ledger outputs never enter any arm's context, notes, or Φ." |
| F11 | AMBER | E1 §7 blind judge isolation (missing no-feedback sentence) | "sees probe question + model answer + ledger entry; does NOT see which arm or run produced the answer." | Ax.1/Ax.2 (external semantic judge). Current isolation (blind, post-hoc, frozen prompt, logged outputs) is correct in structure, but no sentence forbids feedback. A judge that "scores answers" today becomes a policer of acts tomorrow if its output is routed back. | Add one line to E1 §7 and §8: "Judge outputs are logged post-hoc and never enter any arm's context, notes, journal, or Φ, and never cause a write/repeat/reread. Act-log metrics are reported separately and are never scored by the judge." This converts an OK-with-gap into a documented pass. |
| F12 | AMBER | code+SPEC update signaling (`_update` field) | SPEC: "an `_update` field to tool results until the user applies the update." / code: "out[\"_update\"] = notice" (in `call_tool` for `matryoshka_status` and in `handle()` for every `tools/call` result when a notice exists). | Ax.2/Ax.9 (unrequested external content in the memory channel). A version notice appended to every tool result (including `read`) enters the model's context without a model act, through the same channel as `<<PMI>>`. Benign intent, wrong channel. Daily fetch (`check_for_updates`, network to GitHub) is also undisclosed timing behavior. | Restrict to model-requested surface. Change: attach `_update` ONLY to `matryoshka_status` results (and `initialize`), never to `read`/`write`/`repeat`/`connect`/`reconcile`. Document the daily version-string fetch in SPEC §5 header (what is fetched, opt-out `MMI_NO_UPDATE_CHECK=1`). For E1: set `MMI_NO_UPDATE_CHECK=1` in the run manifest (frozen env). |
| F13 | AMBER | code+SPEC injection scope + archive visibility | code: "N loudest WRITE traces by weight only. No relevance, no content" / SPEC: "a `<<PMI>>` block with the N loudest WRITE traces by current weight." / SPEC: "recency reads (`last`) see the active journal only." | Ax.2/Ax.6 (external coverage decisions). WRITE-only injection silently excludes CONNECT/RECONCILE summaries and rules — the model's own consolidations never surface as reminders. `last`-reads blindness to the archive is time/size physics (legitimate dial), but the model is not told coverage changed. E1 text says "N records" (all types) while code injects WRITE only — divergence. | Align text and code, disclose coverage. (a) Decide: either inject loudest N across WRITE+CONNECT+RECONCILE (preferred — surfaces the model's own abstractions) or state "WRITE-only" in E1 §5 and justify (raw episodes only, summaries via explicit `read`). (b) SPEC: add "STATUS reports active/archived counts and the `last`-reads-active-only rule; archive is reachable by `ids`/`range`." Already partly present (`archived` count exists in `call_tool` status) — surface it in the `read` description so the model can compensate by act. |
| F14 | AMBER | preprint §4.3 background tick | "a **background tick** — a low-rate substrate process in dormancy that performs replay and `connect` without external input" | Ax.2 (only K initiates) + Ax.3 (Φ is passive matter, no initiative). A substrate process that "performs connect" issues a semantic act (linking = judging connection) without the core. Sleep-time compute as cited does work without user input, but there the worker is still the model; here the subject is ambiguous. | Disambiguate the subject. Reword to: "Background tick, if enabled, is the core running with no user input at a substrate-set low rate; every `connect` remains the core's act and is journaled as such. A substrate that links by itself (no core act) is NOT PlastFormer and belongs in ablations (`substrate-consolidation on`). E3 tests both configurations separately and reports which counter aged the traces." |
| F15 | AMBER | preprint §3.8 journal hash chain vs implementation; `_rewrite` | "The environment keeps an external append-only journal with a hash chain; entries are written by the environment, never by the model; tampering breaks the chain." | P2 honesty (not strictly axiomatic, but load-bearing for the tamper-evidence claim). No hash chain exists in `mmi_mcp.py` (no hashing at all); `archive_pass` physically rewrites `PHI.jsonl` via tmp+replace and moves records to `PHI-archive.jsonl`. Append-only is logical (fields intact, ids stable), not cryptographic. | Downgrade or implement before arXiv. Either: implement hash-chained journal entries + archive-chain linkage in v0.6.0, or reword to: "Reference executor keeps an append-only JSONL journal without a hash chain (integrity = file custody, not cryptography); hash chaining is specified, not built; P2 is therefore tamper-evidence modulo host trust." Also add: "Archive moves preserve ids/fields; `ids`/`range` reads span active+archive; `record_tick`/`record_time` never mutated by archive passes." |
| F16 | AMBER | E1 ablation presence (RAG ranker, surrogate, wall-clock decay) + Arm B search tool | "injection block selected by amplitude × relevance (embedding similarity to the current user message) instead of amplitude alone" / "embedding-distance surprise gate writes low-amplitude fast records on every exchange" / Arm B: "append-only timestamped notes tool + search tool over own notes" | Ax.2 (external ranker/classifier present in the protocol). As labeled controls with "reported separately, not part of PR1–PR4" the structure is correct; risk is leakage (ablation flag left on, shared harness helper, Arm B search-tool semantics imported into Arm C justification). | Keep, but harden the gate. Add to E1 §5/§10: each ablation is a single env/flag flip from the frozen main config, default off, manifest-logged per run; `RAG-style read` and `unconscious-surrogate` code paths MUST NOT exist in the main-run harness (separate branch/flag file, CI assertion: main-run imports contain no `embedding`/`similarity` symbol); Arm B `search tool` semantics (whose ranking? whose trigger?) specified in one line — if it is relevance-ranked, label it "external baseline, not a PlastFormer act." |

## OK passes (strongest 3 only)

| ID | What passes | Evidence (exact quote) |
|----|-------------|------------------------|
| OK1 | No relevance/similarity ranking in any main path (code, SPEC, E1 main run). | code `inject_top` docstring: "N loudest WRITE traces by weight only. No relevance, no content" + SPEC §6: "Vector/semantic retrieval (the model reads, it does not query an index)." + E1 §5: "Any component that ranks or selects records by relevance to the current query is an external decision-maker about meaning and violates axiom 2" |
| OK2 | Archival is strictly time/size physics, no content conditions. | SPEC §3.1: "Archival is mechanical time/size physics: records move with all fields intact" + code `archive_pass` docstring: "Mechanical archival: size cap and record-time tempo. Time/size physics only — no content decisions, no scoring." + E1 §5: "no edit, no delete, no filtering by weight, no semantic index." |
| OK3 | No auto-write to Φ in the executor; every content record requires a model tool call. | code: storing acts are `act_write` / `act_repeat` / `act_connect` / `act_reconcile` only (each called solely from `call_tool` dispatch on the model's `tools/call`); `archive_pass()` moves but never creates content; `_auto_tick` appends to `TICKS.log`, never to `PHI.jsonl`; in `ticks` mode `act_tick` "appends nothing" (deprecated no-op). SPEC: "The weight is shown, not enforced: no threshold, no scoring, no deletion by content." |

## Fix list (ordered, minimal)

1. E1 §5 + harness: gate `<<PMI>>` on the model's own `read` (F1); forbid pre-turn pushes; or move auto-injection to a named ablation with opt-out and act-log accounting.
2. Preprint §1: remove "timing, delivery" from the outside list; recall timing is semantic (F2); cross-ref F1.
3. Preprint §3.3: unconscious auto-write out of the main claim; parametric-future only with core-owned gate; symbolic surrogate stays ablation (F3).
4. Preprint §3.7 + E1 §5: delete deterministic-extraction auto-write; provenance class asserted by model under pre-registered caps; align $a_0$ text with implementation (uniform until built) (F4, F8).
5. Preprint §3.4: split read-path wording by substrate (push only in parametric; pull-only on the stand) (F5).
6. Freeze tick = per-storing-act (code truth); fix E1 §4/Changelog and preprint mapping note; log ticks-per-exchange in manifest (F6).
7. Code: require explicit `layer` (no silent `episode` default); label fallbacks; update tool descriptions + SPEC example (F7).
8. Friction: "metered, never refused" wording; implement metering log or soften "enforced" (F9).
9. Oracle/ledger/judge: add no-feedback sentences (never enter Φ/context, never trigger acts); E5 held-out biographies (F10, F11).
10. Code: `_update` only on `status`/`initialize`; document fetch + opt-out; E1 manifest sets `MMI_NO_UPDATE_CHECK=1` (F12).
11. Injection scope + archive visibility: WRITE-only vs all-acts decision; disclose `last`-active-only in `read` help (F13).
12. Background tick: subject = core without user input; substrate-self-linking is a separate ablation (F14).
13. Journal: implement hash chain or downgrade P2 to host-trust; document archive id/field preservation (F15).
14. Ablation gates: single-flag flips, default off, manifest-logged; no embedding symbols in main-run path; one-line Arm B search semantics (F16).
15. Editorial (non-blocking): SPEC §1 duplicate sentence ("The model is the only semantic subject" twice); code comment non-English line ("архив читаем") — translate; E1 "N records" vs code "WRITE traces" — unify terms.


---

# Part II — Audit of the axioms themselves (supplement from architect)

Scope: `matryoshka/GLOSSARY.md` axioms 1–9 + `matryoshka/MANIFEST.md` declaration + architectural axioms 1–8.
Question: which wording holes allowed Findings F1–F16 to happen.
Severity here: HOLE = formulation that permits an external decider or contradicts the lived-tick position; CLASH = direct contradiction between two normative texts.

## Gap table (axioms vs MANIFEST)

| ID | Sev | Location | Exact quote | Hole / clash | Which artifact violation it allowed |
|----|-----|----------|-------------|--------------|-------------------------------------|
| C1 | CLASH | MANIFEST Declaration + Proposal vs ADR-001/P1 | "continuous existence in time." + "physical time is given, measured by a clock from the moment of launch." | Directly authorizes wall-clock aging (the rejected design). Contradicts preprint §3.2/§3.5 (Δn in lived ticks, wall stamps weightless) and E1 `ticks`-mandatory. Anyone citing MANIFEST alone can claim calendar decay as compliant. | Would have legitimized E1 ablation `decay in wall-clock` as main path; also undermines F6 fix. |
| C2 | CLASH | MANIFEST Division of roles vs GLOSSARY Ax.9 | "The human provides the physical conditions: the memory volume to be allocated, its persistence, and physical time." | "Provides physical time" reads as owner gives ticks. GLOSSARY Ax.9: "Модель владеет рабочими тактами и сама решает, на что их потратить". Stand counting ticks (F6) and stand-timed injection (F1) both shelter behind this sentence. | F1 (external trigger), F6 (stand counts ticks). |
| C3 | HOLE | GLOSSARY Ax.2 | "Никакого внешнего механизма, распределяющего или считывающего веса за модель, не существует." | Bans only distributing/reading weights. Says nothing about: WHEN a reminder fires (trigger), WHAT surfaces how ranked for presentation, WHO classifies spans for verbatim extraction, WHO sets initial amplitude per source, WHAT the training reward shaped. All four slipped through untested. | F1 (trigger), F4 (extraction classifier), F8 (provenance weights), F10 (oracle training). The axiom needs a trigger/rank/gate/train extension. |
| C4 | HOLE | GLOSSARY Ax.9 | "Модель владеет рабочими тактами и сама решает, на что их потратить: ответ владельцу, композиция памяти, собственные дела." | Enumerates spending targets but omits reminder timing/delivery. Preprint §1 exploited exactly this: "delivery, timing ... may live outside" (F2). No test sentence exists for "a reminder the model did not ask for." | F1, F2, F5, F12 (`_update` piggyback). |
| C5 | CLASH | MANIFEST Nested timescales vs "layers are speeds" | "the architecture only states that the layers exist and that the substrate must be large enough and persistent enough to hold them." | Existential container reading ("layers exist" as places) contradicts GLOSSARY Ax.6 + THEORY "слой — это скорость" + ADR-001. Allows container-branching code (per-layer stores, per-layer vetoes) to claim compliance. | F7 (default layer as placement), F9 (per-component veto priced as place not speed), F13 (WRITE-only injection as container rule). |
| C6 | CLASH | MANIFEST Unique instances vs GLOSSARY Permission 4 + preprint E4 | "a unique corpus of lived experience that cannot be pretrained and cannot be copied." | "Cannot be copied" contradicts GLOSSARY "Копирование Φ — допустимая операция. Копия создаёт ветвь" and preprint E4 migration/transplant. Blocks E4 or forces quiet violation. | E4 test legality; F15 archive/copy handling. |
| C7 | HOLE | GLOSSARY Ax.6 (defaults) | "Дальнейшее членение — решение модели." | Forbids external re-division but not external DEFAULTS. Silent `layer="episode"` when the model says nothing (F7) passes literally: the model did not decide, the executor did, and no sentence says "silence is not consent." | F7. |
| C8 | HOLE | GLOSSARY Ax.7 (bi-temporal, no firewall) | "Каждая запись несёт время события и время узнавания." | Requires stamps but never states the firewall: wall stamps MUST NOT enter amplitude. A wall-clock decay implementation satisfies Ax.7 literally while dissolving P1. | Wall-clock ablation leaking into main path; F6. |
| C9 | HOLE | No training/governance boundary axiom; no evaluation-isolation axiom | (absent — MANIFEST "How Φ is updated ... is the next engineering task and is not disclosed here." is a blank check) | Nothing forbids: training the model toward an oracle then citing overlap as self-governance (F10/E5); routing judge/ledger output back into Φ (F11); substrate self-linking in dormancy (F14); deterministic pre-extractors writing proposals as records (F4). MANIFEST "No act of trust is required for this distinction — it is a property of the input." further denies the trust problem provenance exists to manage. | F4, F10, F11, F14; MANIFEST trust sentence vs §5 threat model. |
| C10 | HOLE | Dials + journal missing at axiom level | (absent — "temperature class" lives only in SPEC/ADR; journal+hash lives only in preprint §3.8) | Without an enumerated-dial axiom, any new knob (volume, tempo, tau-scale, friction tariff, $a_0$ table, inject-N) can claim "physics" with no test. Without a journal axiom, P2 has no normative anchor and `_rewrite`/archive moves (F15) cannot be judged. GLOSSARY Permission 3 even grants the owner "такты" (see C2). | F8, F9, F12, F13, F15. |

## Draft CONSTITUTION.md v1.0 (proposal — included here only, file NOT created)

> NOTE: draft text for `plastformer/docs/CONSTITUTION.md` v1.0. Normative order: this file beats preprint/E1/SPEC on conflicts; GLOSSARY axioms 1–9 and MANIFEST remain lineage, not norm, where they conflict (C1/C2/C5/C6 resolved below in favor of lived ticks, model-owned time, layers-as-speeds, copy-as-branch).

### P1. One semantic subject.
Statement: only the core K performs semantic acts: interpretation, attention, reasoning, intention, action. Φ stores state and does nothing semantic.
Violated if: any non-K component's output determines WHAT a record means, WHETHER it is kept/surfaced, or HOW it is paraphrased — including "deterministic" classifiers, extractors, or pre-filters.
Example: a regex that routes dates/sums to verbatim records without a model act violates; the same regex as a proposal the model must confirm by `name` does not.

### P2. No external decider (trigger / rank / gate / veto).
Statement: no component outside K decides WHEN a memory act fires, WHICH traces surface for the current query, WHAT gate admits a write, or WHETHER a well-formed act executes. The executor executes; it never selects by meaning, never refuses by meaning.
Violated if: a reminder is pushed before a turn the model did not request; records are ordered/filtered by relevance, similarity, keywords, or query-dependence; a write is refused or rewritten for its content; an embedding/similarity symbol exists in the main-run path.
Example: loudest-N block appended to a `read` response the model called is compliant; the same block pasted before every user turn unasked is a violation (belongs in ablations as `loudest-N-auto on`).

### P3. Layers are speeds, not places.
Statement: a layer names a decay time constant τ, never a container, mailbox, or permission zone. All writes deposit across the speed spectrum per the registered τ set; no logic branches on layer-as-location; no veto, quota, or visibility rule keys on layer except the τ value itself.
Violated if: code routes, hides, prices, or refuses by layer name for any reason other than its τ; a default layer is silently assigned and then cited in layer-based claims.
Example: `layer="episode"` default that the executor fills in violates unless recorded as `unspecified (harness default)` and excluded from τ-based conclusions; friction tariff `c(τ)` is compliant, `c(layer-name-as-place)` is not.

### P4. Temperature-class dials only (enumerated, pre-registered, content-blind).
Statement: the owner may set ONLY the dials listed here, each continuous or scalar, each fixed before a run and frozen in the manifest: memory volume (bytes), forgetting tempo (record-age), τ set + τ-scale, friction meter schedule `c(τ)`, provenance cap table, injection N-cap, clock mode. No dial inspects content, assigns meaning, or takes a per-trace decision.
Violated if: a run uses an unlisted knob; a dial value changes mid-run unlogged; any dial's computation reads record content (beyond byte size / timestamps / counters); a "physics" claim has no manifest entry.
Example: `MMI_TAU_TICKS=50,200,1000` frozen pre-run is compliant; an operator retuning τ mid-run to rescue accuracy is a violation (new run required).

### P5. Acts of the model (explicit, complete, logged).
Statement: the ONLY writes to Φ are the model's explicit acts `name / repeat / connect / reconcile` plus explicit `read`; each act is a logged tool call with actor=K. Silence is not consent: missing parameters are errors or recorded `unspecified`, never silently completed by the executor. Every auto-anything (auto-write, auto-remind, auto-link, auto-extract) is NOT an act and belongs in ablations.
Violated if: any Φ record exists without a corresponding logged model act; any "unconscious" write fires without the core's own gate signal; any background process issues `connect`.
Example: dormancy replay that proposes candidates the core then `connect`s by act is compliant; a substrate process writing CONNECT records by itself violates (P1/P2).

### P6. Ticks are lived, counted by the stand, owned by the model.
Statement: abstract tick = one inference step of the instance. Stand counter = one executed storing act (sparse sampling of abstract ticks), advanced ONLY by the stand on WRITE/REPEAT/CONNECT/RECONCILE; READ/STATUS/`tick` calls never advance it. Dormancy is zero lived time unless the core itself works (background tick = core running with no user input, every act journaled as the core's). Wall-clock stamps NEVER enter amplitude.
Violated if: wall seconds affect any weight; `message_no`/exchange count is substituted for the stand counter; idle calendar time ages traces; a manual `tick` call advances the counter in tick mode.
Example: `weight=(1+repeats)·exp(−Δn/τ)`, `Δn=n_now−record_tick` is compliant; `exp(−dt_seconds/τ)` anywhere except the preregistered `decay-in-wall-clock` control ablation violates.

### P7. Provenance is capped physics, asserted by the model.
Statement: per-record source class is asserted by the model in the act call. The deployer's pre-registered cap table bounds initial amplitude only (`a(0) ≤ cap[class]`, uniform until implemented); the executor never reclassifies content and never infers class from content. Class assignment policy is a deployment choice, disclosed, and itself an attack surface.
Violated if: any component rewrites the model's asserted class; amplitude is set from content analysis; the $a_0$ table changes mid-run; a paper claims $a_0$ weighting the executor does not implement.
Example: model asserts `source=user`, executor applies `min(1.0, cap[user])` — compliant; extractor upgrades/downgrades class by scanning dates — violation.

### P8. Journal is append-only, hash-chained, separate from memory.
Statement: the journal is a complete chronological record, written by the environment, never by the model, never edited, never deleted; each entry hash-links the previous; archive moves preserve ids/fields and extend the chain; reads span active+archive by `ids`/`range` (recency-`last` covers active only, disclosed in `read` help and STATUS). Memory (selective, decaying, governed) and journal (complete, immutable, post-mortem) have separate retention/erasure rules.
Violated if: any mutation/deletion of a journal entry; archive move alters fields/ids/ticks; hash chain absent while P2 tamper-evidence is claimed as built; `last`-only blindness undisclosed to the model.
Example: current reference executor (no hash chain) is compliant ONLY while P2 is claimed "modulo host trust"; claiming cryptographic tamper-evidence on it violates.

### P9. Training shapes capacity; governance is post-boundary + held-out.
Statement: training (including oracle-shaped rewards) builds the CAPACITY to perform acts; it MUST NOT decide the CONTENT of any live biography. The oracle/ledger/judge never enter Φ, context, notes, or journal during a run and never trigger/filter acts; they score post-hoc only. Governance is demonstrated on held-out biographies with no oracle present (E1–E3 on novel corpora), never by oracle overlap (E5 bounds, not measures, governance).
Violated if: ledger/judge output reaches any arm's context or Φ; a run is rewarded/filtered mid-run by oracle score; E5 precision/recall is cited as self-governance evidence; training corpora leak into test biographies unreported.
Example: "E5=0.9, therefore self-governed" violates; "E5=0.9 capacity; E1–E3 held-out show governed behavior past the boundary" complies.

### P10. Precedence and honesty labels.
Statement: on conflict, CONSTITUTION.md v1.0 > ADR-001 > preprint/E1/SPEC/code. MANIFEST "continuous calendar existence," "owner provides time," "layers exist as places," "cannot be copied," and "no trust needed" are superseded as norms (retained as lineage). Every report states its configuration point (substrate × topology × act-training), which dials were frozen, which ablations were on, and what is NOT claimed (parametric substrate, trained acts, hash chain, $a_0$ weighting) until built.
Violated if: a result is reported without its axis coordinates and frozen manifest; an ablation flag leaks into a main-run claim; a wall-clock/surrogate/RAG control is cited as PlastFormer behavior.
Example: "Arm C (symbolic × split × prompted, ticks, τ=50/200/1000, N=12, $a_0$=uniform, friction metered-not-refused, surrogate off, RAG-read off) retained X" complies; "memory works" without coordinates violates.

## How Part II maps to the Fix list

C1 → Fix 6 (lived-tick firewall) + P6/P10 supersede note. C2 → Fix 1/6 + P6 (stand counts, model owns). C3 → Fix 1–4 + P2 (trigger/rank/gate/veto ban). C4 → Fix 2 + P2/P5 (reminder timing is semantic). C5 → Fix 7/8/11 + P3. C6 → P10 + E4 wording ("copy creates a branch, not the same instance"). C7 → Fix 7 + P3/P5 (no silent defaults). C8 → Fix 6 + P6 (stamp firewall). C9 → Fix 4/8/9/12 + P5/P9 (training/eval/dormancy gates). C10 → Fix 8/10/13 + P4/P8 (enumerated dials, journal).

## Method note

Read full files (not greps): ADR-001, GLOSSARY axioms 1–9, preprint v0.5, e1-protocol v1.1, mmi_mcp.py v0.6.0, SPEC.md. Checked all 8 assigned points: (1) loudest-N trigger → F1/F2/F5; (2) default layer + content branching → F7 + OK1 (none found besides F4); (3) provenance/friction → F8/F9; (4) archival → OK2; (5) judge feedback → F11 + OK3-structure; (6) E5 oracle → F10; (7) relevance ranking → OK1 + F16 gates; (8) auto-write → F3 + OK3. No files edited. No commit, no push.
