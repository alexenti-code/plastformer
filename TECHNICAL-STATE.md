# PlastFormer — карта технического состояния

**Status:** ACTIVE (v4, 2026-09-11; ядро пересобрано, размер Φ для образца задан владельцем).

## Ядро: что лежит сейчас и что решил владелец

Решением владельца 11.09.2026 **ядром для образца берётся обычный 4-битный чекпойнт**: на этой машине (16 ГБ оперативной памяти) он работает, а QAT — нет.

| Вариант ядра | Размер | Проверка на M1 Pro 16 ГБ |
|---|---|---|
| обычный 4-битный, `experiments/o8-pass/gemma4-12b-text-4bit/` | 6,73 ГБ | загрузка 10,2 с, генерация 17,2 с, пик памяти 6,8 ГБ — **работает** |
| QAT 4-битный, `experiments/o8-pass/gemma4-12b-qat-text-4bit/` | 10,98 ГБ | загрузка 38 с, генерация падает с нехваткой памяти — **не работает** |

QAT-сборка сохранена как вариант для машин с большей памятью. Причина провала арифметическая: 10,95 ГБ весов плюс кэш внимания не влезают в 16 ГБ.

У обоих чекпойнтов из исходных репозиториев вырезаны 17 тензоров зрения и звука: остаётся 1324 текстовых тензора. Разница между обычным и QAT чекпойнтами не в размере слоёв вообще, а в том, что у QAT 144 пер-слойных исключения — MLP всех 48 блоков держатся в 8 битах.

## Что удалено при расчистке 11.09.2026

Освобождено 25,3 ГБ. Удалены: старое ядро проекта, оба дублирующих кэша 4-битного чекпойнта, слитая копия ядра в `/tmp`, копия части веса для опыта записи, бэкапы правленых текстов, мусорная загрузка постороннего репозитория. Свободно на диске стало 53 ГБ вместо 38 ГБ.

## Что удалено ранее (удаляемое наследие; не чинить и не восстанавливать)

Старая раздельная схема «модель отдельно, память отдельно» удалена 10.09.2026 (история — в git):

- `experiments/phi-vector/` — внешний банк векторов и связанные скрипты — удалён целиком;
- `experiments/calibration/` — CAL-1, отдельная стендовая физика — удалён целиком;
- `experiments/e1-run/` — генератор корпуса той же схемы — удалён целиком;
- `experiments/o8-pass/`: скрипты старого цикла и адаптеры — удалены; адаптеры и их конфиги не восстанавливать;
- старый цикл «наполнение банка → обучение проектора → щуп» не является текущей реализацией, прототипом или следующей задачей.

## Что сохранено как сборочный материал (не часть изделия)

- `experiments/o8-pass/material/`, `material-v02/`, `material-v03/` — материал однократного прохода Инструкции;
- `experiments/o8-pass/gemma4-12b-text-4bit/` — **ядро для образца** (обычный 4-битный, 6,73 ГБ);
- `experiments/o8-pass/gemma4-12b-qat-text-4bit/` — QAT-ядро для машин с большей памятью (10,98 ГБ);
- `experiments/o8-pass/gen_material.py` — генератор материала прохода;
- `experiments/act-grammar/` — Инструкция v0.2.0 (канон: `act-grammar-v0.2-ru.md`; пакет вшивания: `INSTRUCTION-PACKAGE.md`);
- `experiments/organ-dataset/` — источник биографий для материала;
- `experiments/project-dataset/` — живой проектный корпус (источник будущего экзамена);
- каталог `models/` пуст: слитая бракованная сборка удалена 10.09.2026.

## Единственный объект работы

Единый файл **A = (K, Φ)** (см. `docs/ARCHITECTURE-FIXED-BRAIN-v1.md`): часть K (4-битные веса ядра с вшитой Инструкцией) + часть Φ (записи Φ1 и Φ2, векторы, амплитуды, прожитые тики, происхождение, метки времени). Вне файла нет состояния экземпляра.

Код сборки файла ещё не написан — его предстоит спроектировать.

## Размер образца: решение владельца (оптимум, 11.09.2026)

| Часть | Размер |
|---|---|
| ядро (обычный 4-битный чекпойнт) | 6,70 ГБ |
| Φ1 (непроизвольная память) | 1,30 ГБ |
| Φ2 (автобиография) | 200 МБ |
| **итого файл** | **8,20 ГБ** |

Хранение — полбайта на число (**1 003 байта** на запись: вектор 1920 чисел по полбайта = 960 байт + 43 байта служебного). Ёмкость: Φ1 — **1 295 114 записей**, Φ2 — **199 401 запись**.

Проверка памяти при работе: 6 701 МБ (веса) + 2 000 МБ (накладные на окно 8к) + 1 500 МБ (Φ) = 10 201 МБ из 12 384 МБ доступных. Запас 2,2 ГБ.

Решения владельца 11.09.2026:
- значения ручек физики хранятся **в шапке Φ** (внутри файла изделия), а не в манифесте: правка переживает прогон;
- **бюджет правок — 3 за прогон**;
- самонастройка в каноне **есть**: правку применяет код (проверка имени ручки, границ и остатка бюджета), человека в этой цепочке нет; заморожен не механизм, а ручка с именем `self_improvement` — её нельзя предложить к изменению.

Согласовано 11.09.2026: **строить якобиев объектив для нашего ядра и замерить рабочую полосу** (раздел 9 плана) — до начала работ по сборке. Результат опыта: есть ли полоса, на каком слое начинается, доходит ли поданный вектор до неё, что подавать — сырой вектор или проекцию на основу. Разбор статьи и выводы — `docs/JSPACE-ANALYSIS.ru.md`.

Полный план сборки — `docs/ASSEMBLY-PLAN.ru.md`. Руководство по выбору размера — `docs/ASSEMBLY-GUIDE.ru.md`.

## Открытые вопросы

- Механизм записи в Φ при работе модели — проверена только запись байт на месте внутри существующего файла; запись во время генерации не проверялась.
- Поведение при заполнении Φ1 (вытеснение по затуханию) — гипотеза, требует наблюдения.
- Качество чтения при хранении в полбайта и при сжатии вектора до 512 чисел — не измерялось.
